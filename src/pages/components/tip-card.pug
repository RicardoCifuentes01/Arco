mixin tip-card(number, url, title, description)
    div(class="tips__tip") 
        div(class="tips__tip__decoration") 
            p(class="tips__tip__number")!= number 
            figure(class="tips__tip__figure") 
                img(src=url, alt=title, title=title)
        h3!= title
        p!= description